{
  "name": "@dryroute/api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "uv run uvicorn dryroute_api.main:app --reload --port 8000",
    "build": "uv sync --frozen",
    "lint": "uv run ruff check .",
    "test": "uv run pytest"
  }
}
